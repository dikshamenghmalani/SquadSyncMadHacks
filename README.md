## Hiker Mesh Simulation

Prototype project that simulates multiple hiker groups forming an ad hoc mesh
network along a trail. Run the main script with `poetry run python main.py` to
generate the visualization and console alerts.

